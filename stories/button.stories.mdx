import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import Button from '../components/button';
import { isCodeShow } from './config';

<Meta title="Button 按钮" component={Button} />

# Button 按钮

常用的操作按钮。

### 基础用法

基础的按钮用法。

<Preview withSource={isCodeShow}>
  <Story name="默认按钮">
    <Button>默认按钮</Button>
  </Story>
</Preview>

### 带箭头

主要用于分页或点击打开小弹窗。

<Preview withSource={isCodeShow}>
  <Story name="带箭头">
    <div>
      <Button up>上一页</Button>
      <Button down>下一页</Button>
    </div>
  </Story>
</Preview>

### 禁用状态

按钮不可用状态。

<Preview withSource={isCodeShow}>
  <Story name="禁用">
    <Button disabled>禁用状态</Button>
  </Story>
</Preview>

### 激活状态

按钮激活状态。

<Preview withSource={isCodeShow}>
  <Story name="激活状态">
    <Button actived>激活状态</Button>
  </Story>
</Preview>

## Attributes

<Props of={Button} exclude={['prefixCls']} />
