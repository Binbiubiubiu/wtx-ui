import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import Button from "../components/button";
import { isCodeShow } from "./config";

<Meta title="公共组件|按钮" component={Button} />

# Button

按钮

## Props

<Props of={Button} />

## Stories

默认

<Preview withSource={isCodeShow}>
  <Story name="默认">
    <Button>默认按钮</Button>
  </Story>
</Preview>

带箭头

<Preview withSource={isCodeShow}>
  <Story name="带箭头">
    <Button up>上一页</Button>
  </Story>
</Preview>

禁用

<Preview withSource={isCodeShow}>
  <Story name="禁用">
    <Button disabled>禁用按钮</Button>
  </Story>
</Preview>

激活

<Preview withSource={isCodeShow}>
  <Story name="激活">
    <Button actived>激活按钮</Button>
  </Story>
</Preview>
