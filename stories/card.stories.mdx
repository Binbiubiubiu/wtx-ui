import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { isCodeShow } from './config';

import Card, { InlineCard } from '../components/card';

<Meta title="Card 卡片" component={Card} />

# Card 卡片

将信息聚合在卡片容器中展示，包含图标、标题和内容。

### 基础用法

展示页面统计信息。

<Preview withSource={isCodeShow}>
  <Story name="基础用法">
    <Card
      icon={<Icon path="assets/number-bar-icons/11.png" width="40" height="46" />}
      title="摄像头类型(种)"
      content="6"
    />
  </Story>
</Preview>

### 内联样式

展示弹窗内统计信息。

<Preview withSource={isCodeShow}>
  <Story name="内联样式">
    <InlineCard title="消防联网设备总数(个)" content="300" />
  </Story>
</Preview>

## Attributes

<Props of={Card} />
