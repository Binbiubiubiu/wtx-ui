import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import { isCodeShow } from './config';
import ImageIcon from '../components/icon';
import ZoomImage from '../public/assets/zoom-btns/qie tu 138 (11).png';

<Meta title="Icon 图标" component={ImageIcon} />

# Icon 图标

### 基础用法

显示图片图标

<Preview withSource={isCodeShow}>
  <Story name="基础用法">
    <ImageIcon path={ZoomImage} />
  </Story>
</Preview>

### 可点击

图标绑定点击事件

<Preview withSource={isCodeShow}>
  <Story name="可点击">
    <ImageIcon path={ZoomImage} width="80" height="80" onClick={action('clicked')} />
  </Story>
</Preview>

## Attributes

<Props of={ImageIcon} />
